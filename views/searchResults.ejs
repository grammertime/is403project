<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Search Result</title>

  <style>
    /* 
      ----------- GENERAL PAGE STYLING -----------
      Matches the look and feel of the Pokémon List page.
    */
    body {
      font-family: "Segoe UI", Arial, sans-serif;
      background-color: #f4f4f9;
      color: #333;
      margin: 0;
      padding: 40px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Main title at the top of the page */
    h1 {
      color: #2c3e50;
      margin-bottom: 30px;
      letter-spacing: 1px;
    }

    /* 
      ----------- CARD STYLING -----------
      Displays search results inside a clean, rounded box.
    */
    .card {
      background: white;
      width: 50%;
      padding: 25px 30px;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);   /* Soft shadow for depth */
      text-align: left;
    }

    /* Pokémon name inside the result card */
    h2 {
      margin-top: 0;
      color: #2c3e50;
    }

    /* Text styling for details */
    p {
      font-size: 15px;
      margin: 8px 0;
    }

    /* Style for “No Pokémon found” message */
    .not-found {
      color: #c0392b;                           /* Soft red to signal not found */
      font-weight: 600;
      text-align: center;
    }

    /* 
      ----------- LINK STYLING -----------
      Styles the “Back to all Pokémon” link like a button.
    */
    a {
      margin-top: 20px;
      display: inline-block;
      text-decoration: none;
      color: white;
      background-color: #4CAF50;
      padding: 8px 14px;
      border-radius: 4px;
      transition: background-color 0.2s ease-in-out;
    }

    /* Slight hover effect on back button */
    a:hover {
      background-color: #43a047;
    }
  </style>
</head>

<body>
  <!-- Page header -->
  <h1>Search Result</h1>

  <!-- 
    Main content box.
    Displays Pokémon info if found, otherwise a "not found" message.
  -->
  <div class="card">
    <% if (pokemon) { %>
      <!-- Display details of the found Pokémon -->
      <h2><%= pokemon.description %></h2>
      <p><strong>ID:</strong> <%= pokemon.id %></p>
      <p><strong>Base Total:</strong> <%= pokemon.base_total %></p>

    <% } else { %>
      <!-- Message if no Pokémon was found -->
      <p class="not-found">No Pokémon found with that name.</p>
    <% } %>
  </div>

  <!-- Link back to the main Pokémon list -->
  <a href="/">← Back to all Pokémon</a>
</body>
</html>
