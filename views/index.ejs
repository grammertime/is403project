<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome - Dashboard</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #e0e7ff 0%, #cffafe 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        }
        
        .welcome-card {
            max-width: 700px;
            width: 100%;
            padding: 3rem;
            border-radius: 1.5rem;
            box-shadow: 0 1rem 3rem rgba(0, 0, 0, 0.15);
            background-color: #fff;
            animation: slideIn 0.5s ease-out;
        }

        @keyframes slideIn {
        from {
                opacity: 0;
                transform: translateY(-30px);
        }
        to {
                opacity: 1;
                transform: translateY(0);
        }
        }
        h1 {
            color: #3b82f6;
            font-weight: 700;
        }

        h2 {
            color: #6b7280;
            font-weight: 600;
        }
        .btn-danger {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            border: none;
            padding: 0.75rem 2rem;
            font-weight: 600;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 0.5rem 1rem rgba(239, 68, 68, 0.4);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="welcome-card">
            <h1 class="text-center mb-4 text-success">Welcome to the Word Count Dashboard</h1>
            <h3 class="text-center mb-4 text-muted">Here to help you reach your word count goal!</h3>
            <p class="text-center mb-4">You have successfully logged in.</p>
            <div class="text-center">
                <a href="/logout" class="btn btn-danger">Logout</a> <!-- calls a logout route not webpage-->
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

